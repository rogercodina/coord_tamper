Coordinates tamper
==================

This drupal is useful when you have a feed source where you have latitude and
longitude on same field separated by a single comma. For example:

45.23434,1.453243

This tamper allows you to select between latitude and longitude so you can use
feeds mappers offered by "Gelocation field" module.
